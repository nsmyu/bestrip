【BesTrip】旅のプランに招待されました

<%= @itinerary.title %>
<%= l @itinerary.departure_date %> 〜 <%= l @itinerary.return_date %>

<%= "#{User.find(@resource.invited_by_id).name}さんがあなたを「#{@itinerary.title}」に招待しています。" %>
下記のリンクから今すぐ参加しましょう。

<%= accept_invitation_url(@resource, invitation_token: @token) %>
