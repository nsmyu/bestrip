
<%= render 'layouts/sidenav' %>
<main class="main-content position-relative">
  <div class="container-fluid py-4 px-1 min-vh-100">
    <div class="mx-auto max-width-800">
      <% if current_user.favorites.blank? %>
        <p class="p-4 text-center text-sm text-dark">お気に入りスポットはありません</p>
      <% else %>
        <%= turbo_frame_tag "favorites", class: "row mx-0", src: favorites_index_lazy_path, loading: :lazy %>
      <% end %>
    </div>
  </div>
</main>
