<% provide :title, "アカウント登録" %>
<main class="main-content mt-0 bg-pearl-white">
  <div class="page-header align-items-start min-vh-100" style="background-image: url(<%= asset_path 'accept_invitation.jpg' %>);">
    <span class="mask bg-white opacity-8"></span>
    <div class="container mt-7 mb-8">
      <div class="row">
        <div class="col-xl-4 col-lg-6 col-md-8 col-12 mx-auto">
          <div class="card z-index-0 fadeIn3 fadeInBottom border-radius-lg">
            <div class="card-header p-0 position-relative mt-n4 mx-3 z-index-2">
              <div class="bg-gradient-primary shadow-primary border-radius-lg py-5 pe-1">
                <h4 class="text-white font-weight-bolder text-center mt-2 mb-0">アカウント登録</h4>
              </div>
            </div>
            <div class="card-body shadow">
              <p class="mb-3 text-sm text-dark">ニックネームとパスワードを設定してアカウント登録をしてください。登録後、招待されたプランに参加することができます。</p>
              <%= form_for(resource, as: resource_name, url: invitation_path(resource_name, invited_itinerary_id: @invited_itinerary.id), html: { method: :put }) do |f| %>
                <%= f.hidden_field :invitation_token, readonly: true %>
                <% if f.object.class.require_password_on_accepting %>
                  <div class="input-group input-group-outline mb-3 <%= "is-filled" if resource[:name].present? %>" >
                    <%= f.label "ニックネーム（20文字以内で入力）", class: "form-label" %>
                    <%= f.text_field :name, class: "form-control w-100", autocomplete: "name", value: nil %>
                    <div class="<%= "error-message" if resource.errors.messages.key?(:name) %>" >
                      <%= resource.errors.full_messages_for(:name).first if resource.errors.any? %>
                    </div>
                  </div>
                  <div class="input-group input-group-outline mb-3">
                    <%= f.label "パスワード（半角英数字6文字以上）", class: "form-label" %>
                    <%= f.password_field :password, class: "form-control w-100", autocomplete: "new-password" %>
                    <div class="<%= "error-message" if resource.errors.messages.key?(:password) %>" >
                      <%= resource.errors.full_messages_for(:password).first if resource.errors.any? %>
                    </div>
                  </div>
                  <div class="input-group input-group-outline mb-3">
                    <%= f.label "パスワード（確認用）", class: "form-label" %>
                    <%= f.password_field :password_confirmation, class: "form-control w-100", autocomplete: "new-password" %>
                    <div class="<%= "error-message" if resource.errors.messages.key?(:password_confirmation) %>" >
                      <%= resource.errors.full_messages_for(:password_confirmation).first if resource.errors.any? %>
                    </div>
                  </div>
                <% end %>
                <div class="text-center">
                  <%= f.submit "アカウント登録", data: { turbo: false }, class: "btn btn-lg bg-gradient-primary w-100 mt-3" %>
                </div>
              <% end %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</main>

